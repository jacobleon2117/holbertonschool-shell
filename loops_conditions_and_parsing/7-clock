#!/usr/bin/env bas
# Using nested loops to diplay the time.
h=0
m=1
while [ $h -le 12 ]
do
    echo "Hour: $h"
    while [ $m -lt 60 ]
    do
        echo "$m"
        m=$((m+1))
    done
    h=$((h+1))
    m=1
done
